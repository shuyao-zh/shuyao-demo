pipeline {
    agent any

    options {
        timestamps()
        disableConcurrentBuilds()
        skipDefaultCheckout(true)
    }

    environment {
        MVN_ARGS = '-Dmaven.gitcommitid.skip=true'
    }

    stages {
        stage('Checkout') {
            steps {
                // Use HTTPS checkout to avoid SSH transport issues on build agents.
                git branch: "${env.BRANCH_NAME ?: 'main'}", url: 'https://github.com/shuyao-zh/shuyao-demo.git'
            }
        }

        stage('Build') {
            steps {
                script {
                    def mavenSettingsArg = fileExists('settings.xml') ? '-s settings.xml' : ''
                    sh "mvn ${mavenSettingsArg} ${env.MVN_ARGS} -DskipTests clean package"
                }
            }
        }

        stage('Test') {
            steps {
                script {
                    def mavenSettingsArg = fileExists('settings.xml') ? '-s settings.xml' : ''
                    sh "mvn ${mavenSettingsArg} ${env.MVN_ARGS} test"
                }
            }
        }
    }

    post {
        always {
            script {
                try {
                    junit allowEmptyResults: true, testResults: '**/surefire-reports/*.xml'
                    archiveArtifacts artifacts: 'target/*.jar', allowEmptyArchive: true
                } catch (Exception ex) {
                    echo "Skipping test report/artifact publish: ${ex.message}"
                }
            }
        }
    }
}